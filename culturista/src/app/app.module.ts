/*

import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { RouterModule } from '@angular/router'; // Import RouterModule
import { AppRoutingModule } from './app-routing.module'; // Import AppRoutingModule

import { AppComponent } from './app.component'; // Standalone component
import { HeaderComponent } from './header/header.component'; // Standalone component
import { LandingComponent } from './landing/landing.component'; // Standalone component
import { FormularioComponent } from './formulario/formulario.component'; // Standalone component

@NgModule({
  imports: [
    BrowserModule,
    RouterModule, // Import RouterModule
    AppRoutingModule, // Import AppRoutingModule
    AppComponent, // Import standalone AppComponent
    HeaderComponent, // Import standalone HeaderComponent
    LandingComponent, // Import standalone LandingComponent
    FormularioComponent // Import standalone FormularioComponent
  ],
  providers: [],
  bootstrap: [AppComponent] // Bootstrap the standalone AppComponent
})
export class AppModule { }*/
